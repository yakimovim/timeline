<Window x:Class="Timeline.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Timeline.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label>Start date</Label>
            <DatePicker x:Name="startPicker" SelectedDateChanged="StartDateChanged" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label>End date</Label>
            <DatePicker x:Name="endPicker" SelectedDateChanged="EndDateChanged" />
        </StackPanel>
        <Canvas x:Name="canvas" Grid.Row="2"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                SizeChanged="CanvasSizeChanged"/>
    </Grid>
</Window>
